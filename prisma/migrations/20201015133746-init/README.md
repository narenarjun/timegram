# Migration `20201015133746-init`

This migration has been generated by Narendran at 10/15/2020, 7:07:46 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."User" (
"id" SERIAL,
"username" text   NOT NULL ,
"email" text   NOT NULL ,
"firstName" text   NOT NULL DEFAULT E'',
"lastName" text   ,
"bio" text   ,
"loginSecret" text   NOT NULL DEFAULT E'',
PRIMARY KEY ("id")
)

CREATE TABLE "public"."Post" (
"id" integer   NOT NULL ,
"location" text   NOT NULL ,
"caption" text   NOT NULL ,
"userId" integer   NOT NULL ,
PRIMARY KEY ("id")
)

CREATE TABLE "public"."Like" (
"id" integer   NOT NULL ,
"userId" integer   NOT NULL ,
"postId" integer   NOT NULL ,
PRIMARY KEY ("id")
)

CREATE TABLE "public"."Comment" (
"id" integer   NOT NULL ,
"text" text   NOT NULL ,
"userId" integer   NOT NULL ,
"postId" integer   NOT NULL ,
PRIMARY KEY ("id")
)

CREATE TABLE "public"."File" (
"id" integer   NOT NULL ,
"url" text   NOT NULL ,
"postId" integer   NOT NULL ,
PRIMARY KEY ("id")
)

CREATE TABLE "public"."Room" (
"id" integer   NOT NULL ,
PRIMARY KEY ("id")
)

CREATE TABLE "public"."Message" (
"id" integer   NOT NULL ,
"text" text   NOT NULL ,
"roomId" integer   NOT NULL ,
"fromId" integer   NOT NULL ,
"toId" integer   NOT NULL ,
PRIMARY KEY ("id")
)

CREATE TABLE "public"."_UserFollow" (
"A" integer   NOT NULL ,
"B" integer   NOT NULL 
)

CREATE TABLE "public"."_RoomToUser" (
"A" integer   NOT NULL ,
"B" integer   NOT NULL 
)

CREATE UNIQUE INDEX "User.username_unique" ON "public"."User"("username")

CREATE UNIQUE INDEX "User.email_unique" ON "public"."User"("email")

CREATE UNIQUE INDEX "_UserFollow_AB_unique" ON "public"."_UserFollow"("A", "B")

CREATE INDEX "_UserFollow_B_index" ON "public"."_UserFollow"("B")

CREATE UNIQUE INDEX "_RoomToUser_AB_unique" ON "public"."_RoomToUser"("A", "B")

CREATE INDEX "_RoomToUser_B_index" ON "public"."_RoomToUser"("B")

ALTER TABLE "public"."Post" ADD FOREIGN KEY ("userId")REFERENCES "public"."User"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "public"."Like" ADD FOREIGN KEY ("userId")REFERENCES "public"."User"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "public"."Like" ADD FOREIGN KEY ("postId")REFERENCES "public"."Post"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "public"."Comment" ADD FOREIGN KEY ("userId")REFERENCES "public"."User"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "public"."Comment" ADD FOREIGN KEY ("postId")REFERENCES "public"."Post"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "public"."File" ADD FOREIGN KEY ("postId")REFERENCES "public"."Post"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "public"."Message" ADD FOREIGN KEY ("roomId")REFERENCES "public"."Room"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "public"."Message" ADD FOREIGN KEY ("fromId")REFERENCES "public"."User"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "public"."Message" ADD FOREIGN KEY ("toId")REFERENCES "public"."User"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "public"."_UserFollow" ADD FOREIGN KEY ("A")REFERENCES "public"."User"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "public"."_UserFollow" ADD FOREIGN KEY ("B")REFERENCES "public"."User"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "public"."_RoomToUser" ADD FOREIGN KEY ("A")REFERENCES "public"."Room"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "public"."_RoomToUser" ADD FOREIGN KEY ("B")REFERENCES "public"."User"("id") ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20201015133746-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,82 @@
+// This is your Prisma schema file,
+// learn more about it in the docs: https://pris.ly/d/prisma-schema
+
+datasource db {
+  provider = "postgresql"
+  url = "***"
+}
+
+generator client {
+  provider = "prisma-client-js"
+}
+
+model User {
+  id               Int       @id @default(autoincrement())
+  username         String    @unique
+  email            String    @unique
+  firstName        String    @default("")
+  lastName         String?
+  bio              String?
+  following        User[]    @relation("UserFollow", references: [id])
+  followers        User[]    @relation("UserFollow", references: [id])
+  posts            Post[]
+  likes            Like[]
+  comments         Comment[]
+  rooms            Room[]
+  loginSecret      String    @default("")
+  SentMessages     Message[] @relation("sentMessages")
+  ReceivedMessages Message[] @relation("receivedMessages")
+}
+
+model Post {
+  id       Int       @id
+  location String
+  caption  String
+  user     User      @relation(fields: [userId], references: [id])
+  files    File[]
+  likes    Like[]
+  comments Comment[]
+  userId   Int
+}
+
+model Like {
+  id     Int  @id
+  user   User @relation(fields: [userId], references: [id])
+  post   Post @relation(fields: [postId], references: [id])
+  userId Int
+  postId Int
+}
+
+model Comment {
+  id     Int    @id
+  text   String
+  user   User   @relation(fields: [userId], references: [id])
+  post   Post   @relation(fields: [postId], references: [id])
+  userId Int
+  postId Int
+}
+
+model File {
+  id     Int    @id
+  url    String
+  post   Post   @relation(fields: [postId], references: [id])
+  postId Int
+}
+
+model Room {
+  id           Int       @id
+  participants User[]
+  messages     Message[]
+}
+
+model Message {
+  id     Int    @id
+  text   String
+  room   Room   @relation(fields: [roomId], references: [id])
+  roomId Int
+  from   User   @relation(name: "sentMessages", fields: [fromId], references: [id])
+  fromId Int
+  to     User   @relation(name: "receivedMessages", fields: [toId], references: [id])
+  toId   Int
+
+}
```


